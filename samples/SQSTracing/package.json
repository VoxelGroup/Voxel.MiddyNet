{
  "name": "voxel.middynet.sqstracingsample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-package": "pushd Voxel.MiddyNet.SQSTracingSample && build.cmd && popd",
    "deploy": "pushd Voxel.MiddyNet.SQSTracingSample && serverless deploy && popd"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^2.40.0"
  }
}
