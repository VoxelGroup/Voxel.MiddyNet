{
  "name": "voxel.middynet.apigatewaytracingsample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-package": "pushd Voxel.MiddyNet.ApiGatewayTracingSample && build.cmd && popd",
    "deploy": "pushd Voxel.MiddyNet.ApiGatewayTracingSample && serverless deploy && popd"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^2.40.0"
  }
}
