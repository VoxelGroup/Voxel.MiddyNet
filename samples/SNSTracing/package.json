{
  "name": "voxel.middynet.snstracingsample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-package": "pushd Voxel.MiddyNet.SNSTracingSample && build.cmd && popd",
    "deploy": "pushd Voxel.MiddyNet.SNSTracingSample && serverless deploy && popd"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^2.40.0"
  }
}
